[build]
builder = "NIXPACKS"
buildCommand = "npm install --include=dev && npm run build --workspace @atar/api"

[deploy]
startCommand = "npm run start:prod --workspace @atar/api"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
